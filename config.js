module.exports = {
  /**
   * If you want to download the citations of specific label of your library, go this label and copy the number after `scilib=` in the url.
   * Default: 1 (Also if nolabelId is configured).
   */
  labelId: 1025,

  /**
   * The path where you want to store your bib file.
   * Default: "."
   */
  outPath: "C:/Users/morge/Documents",

  /**
   * The filename downloaded citations file.
   * Default: "citations.bib"
   */
  fileName: "citations.bib"
};
